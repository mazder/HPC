# Install Kokkos
 $git clone https://github.com/kokkos/kokkos.git
 $cd kokkos
 $mkdir build
 $cd build
 $cmake .. -DCMAKE_CXX_COMPILER=g++ -DCMAKE_INSTALL_PREFIX=/home/mrahman/kokks_install -DKokkos_ENABLE_OPENMP=ON
 $make && make install 
